# 前端对接文档 — 用户粘性功能

## 一、WebSocket 新增消息场景

### 1.1 主动推送消息（AI 先发制人）

之前 WebSocket 只有用户先发消息、AI 再回复的模式。现在 AI 会**主动推送**消息到客户端，前端需要处理"非用户触发"的 incoming 消息。

**触发场景：**

| 场景 | 推送时间 | 消息特征 |
|------|---------|---------|
| 个性化主动消息 | 每天 9:00 | `sender: "ai"`, `type: "TEXT"` |
| 思念消息 | 每天 14:00 / 20:00 | `sender: "ai"`, `type: "TEXT"`, `animation.emotion` 通常为 `sad` 或 `worried` |
| 角色日记 | 每天 23:00 | `sender: "ai"`, `type: "TEXT"`, 内容以 `📖 角色名的日记：\n` 开头 |
| 生活事件 | 每天 8:00 / 12:00 / 18:00 / 22:00 | `sender: "ai"`, `type: "TEXT"` |

**消息格式（与现有 TEXT 消息完全一致）：**

```json
{
  "type": "TEXT",
  "content": "你今天怎么没来找我呀...有点想你了",
  "sender": "ai",
  "animation": {
    "emotion": "sad"
  }
}
```

**前端处理要点：**

- 这些消息是服务端主动推送的，不是用户发送消息后的回复
- 建议在聊天界面中**与普通 AI 回复区分展示**，例如：
  - 主动消息：左侧气泡 + 小标签「主动找你」
  - 日记消息：特殊卡片样式（📖 图标 + 折叠展开）
  - 生活事件：带时间戳的动态卡片
- 如果用户当前不在聊天页面，建议用**本地通知 / 红点提示**提醒

---

### 1.2 WebSocket 连接后的初始化建议

连接成功后（收到 `CONTROL: connected`），建议前端立即请求以下接口获取未读信息：

```javascript
// 1. 获取未读日记数量
GET /api/auto-chat/diary/unread-count
// Response: { "code": 200, "data": 3 }

// 2. 获取好感度信息
GET /api/auto-chat/relationship
// Response: { "code": 200, "data": { "favorability": 65, "levelName": "亲密", "totalInteractions": 42 } }

// 3. 获取日记列表
GET /api/auto-chat/diary?limit=10
// Response: { "code": 200, "data": [...] }
```

---

## 二、新增 REST API 接口

### 2.1 获取角色日记列表

```
GET /api/auto-chat/diary?limit=10
```

**请求头：**
```
Authorization: Bearer <jwt_token>
```

**响应：**
```json
{
  "code": 200,
  "data": [
    {
      "id": 1,
      "sessionId": "xxx",
      "userId": "xxx",
      "sender": "ai",
      "messageType": "diary",
      "content": "今天他又没来找我，是不是我哪里做得不好...",
      "createTime": "2026-05-19T23:00:00"
    }
  ]
}
```

**前端展示建议：**
- 日记卡片样式，带日期和 📖 图标
- 支持折叠/展开
- 按时间倒序排列

---

### 2.2 获取未读日记数量

```
GET /api/auto-chat/diary/unread-count
```

**请求头：**
```
Authorization: Bearer <jwt_token>
```

**响应：**
```json
{
  "code": 200,
  "data": 3
}
```

**前端展示建议：**
- 在聊天入口显示红点/数字角标
- 类似微信的未读消息提示

---

### 2.3 获取好感度信息

```
GET /api/auto-chat/relationship
```

**请求头：**
```
Authorization: Bearer <jwt_token>
```

**响应：**
```json
{
  "code": 200,
  "data": {
    "favorability": 65,
    "levelName": "亲密",
    "totalInteractions": 42
  }
}
```

**字段说明：**

| 字段 | 类型 | 说明 |
|------|------|------|
| favorability | int | 好感度值（0-100） |
| levelName | string | 等级名称：陌生 / 熟悉 / 亲密 / 羁绊 |
| totalInteractions | int | 累计互动次数 |

**好感度等级对照：**

| 好感度范围 | 等级 | 建议展示 |
|-----------|------|---------|
| 0-20 | 陌生 | 灰色心形，1颗星 |
| 21-50 | 熟悉 | 蓝色心形，2颗星 |
| 51-80 | 亲密 | 粉色心形，3颗星 |
| 81-100 | 羁绊 | 红色心形，4颗星 + 特效 |

**前端展示建议：**
- 在角色资料页/聊天页顶部展示好感度进度条
- 好感度变化时播放动画效果（心形飘起、进度条增长）
- 不同等级解锁不同的 UI 元素（如专属称呼、特殊表情包框）

---

## 三、WebSocket 消息类型扩展

### 3.1 现有消息类型（不变）

| type | 说明 | 方向 |
|------|------|------|
| TEXT | 文本消息 | 双向 |
| IMAGES | 图片消息 | 服务端→客户端 |
| CONTROL | 控制命令 | 双向 |
| ERROR | 错误消息 | 服务端→客户端 |
| PING/PONG | 心跳 | 双向 |
| AUDIO | 音频 | 服务端→客户端 |

### 3.2 消息 messageType 的新值（数据库层面）

聊天消息的 `messageType` 字段新增了两个值，这些值出现在 REST API 返回的聊天记录中：

| messageType | 说明 | 前端建议处理 |
|-------------|------|------------|
| `text` | 普通文本（原有） | 正常展示 |
| `image` | 图片消息（原有） | 正常展示 |
| `audio` | 语音消息（原有） | 正常展示 |
| `smart_memory` | AI 智能记忆（新增） | **不需要展示给用户**，这是 AI 内部记忆 |
| `diary` | 角色日记（新增） | 特殊卡片样式展示 |
| `life_event` | 生活事件（新增） | 带时段标签的动态展示 |

> **注意：** WebSocket 推送的日记和生活事件消息的 `type` 仍然是 `TEXT`，不是 `diary` 或 `life_event`。
> `messageType` 字段仅存在于 MySQL 存储和 REST API 返回中。
> 如果需要在前端区分，可以通过**内容特征**判断：
> - 日记：内容以 `📖` 开头
> - 生活事件：需要后端额外传递标记（见下方扩展建议）

---

## 四、前端 UI 改造建议

### 4.1 聊天页面改造

```
┌─────────────────────────────────┐
│  角色名    ❤️ 亲密 65/100  [📖3] │  ← 新增：好感度 + 未读日记
├─────────────────────────────────┤
│                                 │
│  [📖 龙宝的日记]                │  ← 新增：日记卡片
│  今天他又没来找我...             │
│  2026-05-19 23:00               │
│                                 │
│  [🌅 生活动态]                   │  ← 新增：生活事件
│  刚吃完午饭，好撑～              │
│  12:00                          │
│                                 │
│           用户的消息  →          │  ← 原有
│  ←  AI的回复                    │  ← 原有
│                                 │
├─────────────────────────────────┤
│  [输入框]              [发送]    │  ← 原有
└─────────────────────────────────┘
```

### 4.2 好感度组件

```html
<!-- 建议实现 -->
<div class="relationship-bar">
  <span class="level-icon">💕</span>
  <span class="level-name">亲密</span>
  <div class="progress-bar">
    <div class="progress-fill" style="width: 65%"></div>
  </div>
  <span class="favorability-value">65/100</span>
</div>
```

**好感度变化动画建议：**
- 好感度增加：进度条平滑增长 + 心形粒子飘起
- 好感度减少：进度条变红 + 震动效果
- 等级提升：全屏庆祝动画 + 解锁提示

### 4.3 日记面板

建议新增一个日记入口（类似微信朋友圈图标），点击后展示日记列表：

```
┌─────────────────────────────────┐
│  ← 龙宝的日记                    │
├─────────────────────────────────┤
│                                 │
│  📖 2026-05-19                  │
│  今天他又没来找我，是不是我哪里   │
│  做得不好...算了，明天再看看吧。  │
│                                 │
│  ─────────────────────────      │
│                                 │
│  📖 2026-05-18                  │
│  他今天跟我说了好多话，好开心！   │
│  希望明天也能这样...             │
│                                 │
└─────────────────────────────────┘
```

### 4.4 主动消息通知

当用户不在聊天页面时收到主动推送消息：

```
┌─────────────────────────────────┐
│  🔔 龙宝找你啦                   │
│  "你今天怎么没来找我呀..."       │
│                    [去聊聊 →]    │
└─────────────────────────────────┘
```

**实现方式：**
- Web 端：使用 `Notification API` 发送浏览器通知
- 小程序端：使用微信模板消息 / 订阅消息（需要后端配合接入微信消息推送）

---

## 五、WebSocket 消息处理代码示例

### 5.1 处理主动推送消息

```javascript
// WebSocket onMessage 回调中
function handleMessage(event) {
  const msg = JSON.parse(event.data);

  switch (msg.type) {
    case 'TEXT':
      // 判断是否为主动推送（非用户触发的回复）
      const isProactive = !isWaitingForResponse;

      if (isProactive) {
        // 主动推送消息 - 特殊展示
        showProactiveMessage(msg.content, msg.animation?.emotion);
        // 发送浏览器通知
        sendNotification(msg.content);
      } else {
        // 正常 AI 回复 - 原有逻辑
        showAIResponse(msg.content, msg.animation?.emotion);
      }
      break;

    case 'IMAGES':
      showImage(msg.content);
      break;

    case 'CONTROL':
      handleControl(msg.content);
      break;
  }
}
```

### 5.2 判断消息类型（日记/生活事件/普通消息）

```javascript
function categorizeMessage(content) {
  if (content.startsWith('📖')) {
    return 'diary';
  }
  // 如果后端未来在 WebSocketMessage 中增加 subType 字段，可以直接判断
  return 'normal';
}
```

### 5.3 好感度展示

```javascript
async function loadRelationship() {
  const res = await fetch('/api/auto-chat/relationship', {
    headers: { 'Authorization': `Bearer ${token}` }
  });
  const { data } = await res.json();

  // 更新 UI
  updateFavorabilityBar(data.favorability, data.levelName);
}

function updateFavorabilityBar(value, levelName) {
  const bar = document.querySelector('.progress-fill');
  bar.style.width = `${value}%`;

  // 根据等级设置颜色
  const colors = {
    '陌生': '#9ca3af',
    '熟悉': '#60a5fa',
    '亲密': '#f472b6',
    '羁绊': '#ef4444'
  };
  bar.style.backgroundColor = colors[levelName] || '#9ca3af';
}
```

---

## 六、建议后端后续优化（前端需要配合）

### 6.1 WebSocketMessage 增加 subType 字段

当前 WebSocket 推送的消息 `type` 只有 `TEXT`，无法区分日记、生活事件和普通消息。建议后端在 `WebSocketMessage` 中增加 `subType` 字段：

```json
{
  "type": "TEXT",
  "subType": "diary",        // 新增：normal / diary / life_event / miss_you
  "content": "...",
  "sender": "ai",
  "animation": { "emotion": "sad" }
}
```

这样前端可以精确区分消息类型，无需通过内容特征猜测。

### 6.2 好感度变化实时推送

当好感度发生变化时，通过 WebSocket 推送变化事件：

```json
{
  "type": "CONTROL",
  "content": "favorability_changed",
  "data": {
    "favorability": 68,
    "delta": 3,
    "levelName": "亲密"
  }
}
```

前端收到后实时更新进度条和动画。

---

## 七、配置项汇总

以下配置项控制后端功能的开关和定时，前端无需修改，但需要了解其行为：

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| `auto.chat.enabled` | false | 总开关：主动消息功能 |
| `auto.chat.miss-you.enabled` | false | 思念消息开关 |
| `auto.chat.cron` | `0 0 9 * * ?` | 主动消息发送时间（每天9点） |
| `auto.chat.miss-you.cron` | `0 0 14,20 * * ?` | 思念消息发送时间（14点和20点） |
| `auto.chat.diary-cron` | `0 0 23 * * ?` | 日记生成时间（每晚23点） |
| `auto.chat.schedule-cron` | `0 0 8,12,18,22 * * ?` | 生活事件推送时间 |
| `auto.chat.decay-cron` | `0 0 3 * * ?` | 好感度衰减时间（凌晨3点） |
| `auto.chat.miss-threshold-hours` | 2 | 多久没聊天触发思念（小时） |
| `auto.chat.anxious-threshold-hours` | 24 | 多久触发焦虑情绪（小时） |
| `auto.chat.cold-threshold-hours` | 72 | 多久触发冷淡情绪（小时） |

---

## 八、完整数据流图

```
用户打开页面
    │
    ├── WebSocket 连接 /ws/chat?token=xxx
    │       │
    │       ├── 收到 CONTROL: connected
    │       │
    │       ├── GET /api/auto-chat/relationship → 渲染好感度条
    │       ├── GET /api/auto-chat/diary/unread-count → 显示红点
    │       │
    │       │   ...用户正常聊天...
    │       │
    │       ├── 收到 TEXT (AI回复) → 渲染气泡 + Live2D动画
    │       ├── 收到 IMAGES → 渲染图片
    │       │
    │       │   ...AI 主动推送（用戶无操作）...
    │       │
    │       ├── 收到 TEXT (思念消息) → 特殊气泡 + 通知
    │       ├── 收到 TEXT (日记) → 日记卡片 + 通知
    │       ├── 收到 TEXT (生活事件) → 动态卡片
    │       │
    │       └── 连接关闭
    │
    └── 日记面板入口
            │
            └── GET /api/auto-chat/diary → 日记列表页
```
